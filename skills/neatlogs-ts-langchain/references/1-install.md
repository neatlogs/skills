# Step 1: Install neatlogs

1. Call `detect_package_manager` for the install command.
2. Install `neatlogs` (npm: `npm install neatlogs`, pnpm: `pnpm add neatlogs`, yarn: `yarn add neatlogs`, bun: `bun add neatlogs`).
3. Node.js >= 18 required.

`neatlogs` appears in `package.json`. Proceed.
