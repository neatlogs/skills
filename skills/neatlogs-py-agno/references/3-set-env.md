# Step 3: Set Environment Variables

1. `set_env_values`: `NEATLOGS_API_KEY`.
2. Preserve all `.env.example` keys (e.g. `OPENAI_API_KEY` for `OpenAIChat`). `.env` is a superset.
3. `.env` gitignored.
